/**
 * Copy to lookup-config.js
 *
 * Preferred: run `npm start` and set YOUTUBE_DATA_API_KEY in the environment (.env.example).
 * The browser then calls /api/channel (no key in the page).
 *
 * Fallback: client-side YouTube API (key below) if the server is not available.
 */
window.LOOKUP_API_BASE = '';
window.YOUTUBE_DATA_API_KEY = '';
